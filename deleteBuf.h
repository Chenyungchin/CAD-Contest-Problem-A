#ifndef DELETEBUF_H
#define DELETEBUF_H
#include <bits/stdc++.h>
#include <array>
#include "graph.h"

using namespace std;

void deleteBuf(vector<tuple<string, Gate *>> primary_inputs, vector<tuple<string, Gate *>> primary_outputs);



#endif