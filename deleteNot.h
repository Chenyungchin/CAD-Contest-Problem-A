#ifndef DELETENOT_H
#define DELETENOT_H
#include <bits/stdc++.h>
#include <array>
#include "graph.h"

using namespace std;

void deleteNot(map<string, Gate *> primary_inputs, map<string, Gate *> primary_outputs);



#endif